# Agate API

This repository is a **local Docker demo** only. Run the API via root `make up` (see [README.md](../../README.md)).

For a production deployment you would supply your own hosting (e.g. container platform), set `DATABASE_URL`, `REDIS_URL`, `MASTER_ENCRYPTION_KEY`, `UI_ORIGIN`, and add authentication; that is not configured in this demo tree.
