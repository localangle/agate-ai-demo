# Agate UI – deploy notes

Build a static bundle with `npm run build` and set `VITE_API_BASE` to your API URL. See [README.md](./README.md).

This demo repo does not include S3/CloudFront or CI configuration.
