# Deployment

This repository is intended as a **local Docker demo**. Use `make up` / `make down` from the root [README.md](./README.md).

There is no bundled production path (no Copilot/Terraform in this tree).
